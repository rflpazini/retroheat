// The window chrome is a registry component (see web/registry.json); the app
// imports it from here so a rename in one place reaches every page.
export { Window, TitleBar } from '@/components/retro-os/window'
