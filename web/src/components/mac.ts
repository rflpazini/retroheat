/*
  The classic Mac controls the dialogs share. A push button is a rounded
  rectangle that inverts while pressed; the default one is wrapped in the
  thick ring that answered Return. A field is a sunken well.
*/
export const macButton =
  'min-w-[4.5rem] rounded-md border border-[var(--border)] bg-[var(--card)] px-4 py-1 text-xs font-semibold active:bg-[var(--border)] active:text-[var(--card)]'

export const defaultRing = 'rounded-[10px] border-[3px] border-[var(--border)] p-[2px]'

export const macField = 'bevel-in border-2 border-[var(--border)] bg-[var(--card)] px-2 py-1.5 text-xs outline-none'
