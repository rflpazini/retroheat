# Optional. Both values are public by design (the anon key ships in the bundle;
# row-level security in the database is the real boundary). Leave them empty
# and the site runs exactly as before, with the account features hidden.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
