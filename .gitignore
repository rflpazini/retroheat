web/node_modules/
web/dist/
web/.vite/
# Built shadcn registry, generated by `npm run registry`.
web/public/r/
web/*.tsbuildinfo
*.tmp
.DS_Store
.env

# Raw listing archives written by `collector -raw-dir`; they are published as
# release assets, not committed.
/raw/

# Built binaries. These are anchored to the repository root on purpose: an
# unanchored "collector" would also match the cmd/collector source directory.
/retroheat
/collector
